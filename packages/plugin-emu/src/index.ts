/* @enchanter-ai/plugin-emu — thin re-export shell.
   Canonical implementation lives in the root `enchanter` package at
   src/plugins/emu.adapter.ts. */

export { emuAdapter, configureEmu } from 'enchanter';
