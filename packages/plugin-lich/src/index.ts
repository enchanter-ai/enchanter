/* @enchanter-ai/plugin-lich — thin re-export shell.
   Canonical implementation lives in the root `enchanter` package at
   src/plugins/lich.adapter.ts. */

export { lichAdapter, configureLich } from 'enchanter';
