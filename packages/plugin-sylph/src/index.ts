/* @enchanter-ai/plugin-sylph — thin re-export shell.
   Canonical implementation lives in the root `enchanter` package at
   src/plugins/sylph.adapter.ts. */

export { sylphAdapter } from 'enchanter';
