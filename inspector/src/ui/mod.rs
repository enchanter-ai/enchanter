//! Shared UI primitives used across every view.
//!
//! - `theme`   — palette, severity colors, plugin accent map
//! - `widgets` — reusable composite widgets (sparklines, badges, tables)
//! - `layout`  — chrome (header, footer, status bar) and split helpers

pub mod theme;
pub mod widgets;
pub mod layout;
